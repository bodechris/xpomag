packages:
  - "apps/*"
  - "packages/*"

# pnpm 10+/11 blocks dependency lifecycle scripts unless explicitly approved.
# These packages require their install/build scripts for native/platform binaries.
allowBuilds:
  "@parcel/watcher": true
  "esbuild": true
  "msgpackr-extract": true
